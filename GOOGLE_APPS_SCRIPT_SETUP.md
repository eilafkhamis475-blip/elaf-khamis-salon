# إعداد Google Apps Script وWhatsApp Business لصالون إيلاف خميس

يربط هذا الدليل منصة الحجز بملف Google Sheets وبـ Google Calendar الخاص بخبيرة التصفيف، ثم يرسل **إشعاراً تلقائياً إلى واتساب الإدارة** عند إنشاء حجز جديد. لا يعتمد التشغيل على Make.com. يتم حفظ مفاتيح Google وMeta داخل **Script Properties** فقط، ولا تصل إطلاقاً إلى الصفحة العامة أو متصفح الزبونة.

> يستخدم ملف Google Sheets المقدم المعرّف `1Okg27m5PNVfe8Rr2Zw5rAevPP8p5SVx1mt6_WZn0jyY`. ينشئ السكربت ورقة باسم **الحجوزات** ويضيف رؤوس الأعمدة تلقائياً عند أول تشغيل.

| مرحلة العملية | النتيجة |
|---|---|
| حفظ الحجز من الموقع | يتحقق السكربت من السر المشترك ويضيف أو يحدّث صف الحجز. |
| تنظيم الموعد | ينشئ حدث Google Calendar بعنوان **اسم الزبونة - نوع الخدمة**. |
| إشعار الإدارة | يرسل السكربت قالب WhatsApp Business Utility معتمداً إلى رقم الإدارة `218922119292`. |
| فشل واتساب | يبقى الحجز محفوظاً في Sheet وCalendar؛ فشل الإشعار لا يلغي الحجز. |

## 1. إنشاء مشروع Google Apps Script

افتحي [Google Apps Script](https://script.google.com/home)، ثم اختاري **New project**. غيّري اسم المشروع إلى `Elaf Khamis Salon Booking Sync`. افتحي الملف `Code.gs`، احذفي محتواه، والصقي المحتوى الكامل من الملف المرفق `docs/google-apps-script/Code.gs`.

من القائمة العلوية اختاري الدالة `initialiseSalonBookingsSheet` ثم اضغطي **Run**. وافقي على صلاحيات الوصول إلى Google Sheets وGoogle Calendar؛ هذه الصلاحيات مطلوبة لإضافة الصفوف وإنشاء أحداث المواعيد.

بعد ذلك اختاري الدالة `generateSharedSecret` ثم اضغطي **Run**. افتحي **Execution log** وانسخي القيمة الطويلة التي ظهرت. لا تضعي هذه القيمة في الكود أو في مستند عام.

## 2. إعداد WhatsApp Business Cloud API لدى Meta

يتطلب الإرسال البرمجي المباشر حساب WhatsApp Business Platform ورقم أعمال **مرسِل** مهيّأ داخل تطبيق Meta. الإشعار الآلي الذي قد يقع خارج نافذة خدمة العملاء مدتها 24 ساعة يجب أن يستخدم قالباً معتمداً، لذلك يعتمد الكود على قالب من فئة **Utility**.[1] [2]

افتحي [Meta for Developers](https://developers.facebook.com/) وأنشئي تطبيقاً من نوع **Business**، ثم أضيفي إليه منتج **WhatsApp**. من إعدادات WhatsApp أضيفي رقم الأعمال المرسل ونسخي قيمة **Phone number ID** الخاصة به. أنشئي رمز وصول دائم لحساب النظام الذي لديه صلاحية إرسال رسائل WhatsApp، ولا تستخدمي رمز الاختبار المؤقت إلا للتحقق الأولي.

> يجب أن يكون رقم الإرسال في Cloud API **مختلفاً** عن رقم الاستلام `218922119292`. لا يمكن الاعتماد على إرسال رسالة من رقم الأعمال إلى الرقم نفسه. إذا كان الرقم `0922119292` هو رقم الإدارة الذي ستستقبل منه الإشعارات، استخدمي رقماً آخر مخصصاً للإرسال في Cloud API، واجعلي `218922119292` هو المستلم.

### إنشاء قالب الإشعار العربي

من **WhatsApp Manager → Message templates** أنشئي قالباً جديداً من نوع **Utility**. سمّيه مثلاً `salon_new_booking` واضبطي اللغة على العربية (`ar`). يجب أن يكون النص التالي في جسم القالب، وبالمتغيرات الثمانية نفسها وبهذا الترتيب:

```text
👑 طلب حجز جديد — صالون إيلاف خميس

الزبونة: {{1}}
الخدمة: {{2}}
الموعد: {{3}}
مكان التجهيز: {{4}}
عدد الأفراد: {{5}}
هاتف الزبونة: {{6}}
رابط الموقع: {{7}}
رقم الحجز: {{8}}

يرجى مراجعة تفاصيل الموعد والتواصل مع الزبونة عند الحاجة.
```

أدخلي أمثلة للقيم عند طلبها في واجهة Meta، ثم أرسلي القالب للمراجعة. لا تنتقلي إلى الاختبار الفعلي قبل ظهور حالة القالب **Approved**. تعتمد واجهة Meta المتغيرات الموضعية بالترتيب عند إرسال القالب، ولذلك يجب عدم تغيير ترتيب `{{1}}` إلى `{{8}}` من دون تعديل الكود.[1]

## 3. إضافة Script Properties

من **Project Settings → Script properties** أضيفي الخصائص التالية. لا تضعي أي قيمة حساسة في ملف `Code.gs` نفسه.

| اسم الخاصية | القيمة المطلوبة |
|---|---|
| `SALON_WEBHOOK_SECRET` | القيمة التي أنشأتها دالة `generateSharedSecret`. |
| `SALON_CALENDAR_ID` | معرّف تقويم المصففة، أو اتركيه فارغاً لاستخدام التقويم الرئيسي للحساب. |
| `WHATSAPP_ACCESS_TOKEN` | رمز الوصول الدائم من Meta لحساب النظام المصرّح له بالرسائل. |
| `WHATSAPP_PHONE_NUMBER_ID` | قيمة **Phone number ID** لرقم Cloud API المرسل، وليست رقم الهاتف نفسه. |
| `WHATSAPP_OWNER_PHONE` | `218922119292` أو `00218922119292`؛ يحوّله الكود تلقائياً إلى الصيغة الدولية من دون `+` أو `00`. |
| `WHATSAPP_TEMPLATE_NAME` | اسم القالب المعتمد، مثل `salon_new_booking`. |
| `WHATSAPP_TEMPLATE_LANGUAGE` | `ar`، أو رمز اللغة المطابق للقالب إن عرضته Meta بصورة أخرى. |
| `WHATSAPP_GRAPH_API_VERSION` | `v23.0`، واتركيها كما هي ما لم تطلبي تحديثاً متوافقاً مع Meta. |

للحصول على معرّف تقويم غير رئيسي: افتحي Google Calendar في المتصفح، ثم **Settings**، اختاري التقويم المطلوب، وانسخي قيمة **Calendar ID** من قسم **Integrate calendar**.

## 4. نشر Web App والحصول على الرابط

اختاري **Deploy → New deployment**. من نوع النشر اختاري **Web app**. اضبطي **Execute as** على `Me` لكي يستخدم السكربت صلاحيات صاحبة الحساب للوصول إلى Google Sheets وGoogle Calendar. اضبطي **Who has access** على `Anyone` لأن خادم المنصة ليس مستخدماً مسجلاً في حساب Google الخاص بك؛ الحماية الفعلية تتحقق عبر `SALON_WEBHOOK_SECRET` داخل كل طلب.

اضغطي **Deploy** ووافقي على الصلاحيات، ثم انسخي **Web app URL** المنتهي بـ `/exec`. لا تستخدمي رابط `/dev` لأنه مخصص للاختبارات فقط ويتطلب صلاحية تحرير للمشروع.[3]

بعد أي تغيير في `Code.gs`، اختاري **Deploy → Manage deployments → Edit → New version → Deploy**. لا يكفي حفظ الملف فقط لتشغيل النسخة الجديدة على رابط `/exec`.

## 5. ربط المنصة بالخادم

تظهر هذه القيم في إعدادات أسرار المنصة، وقد تم إعدادها سلفاً في هذا المشروع:

| حقل المنصة | القيمة |
|---|---|
| `GOOGLE_APPS_SCRIPT_WEB_APP_URL` | رابط النشر المنتهي بـ `/exec`. |
| `GOOGLE_APPS_SCRIPT_SHARED_SECRET` | قيمة `SALON_WEBHOOK_SECRET` ذاتها. |

يستدعي خادم المنصة Google Apps Script لإجراء فحص تعارض في Google Calendar ضمن هامش ثلاث ساعات، ثم ينشئ أو يحدّث صف الحجز وحدث التقويم. لا تتغير تجربة الزبونة: بعد الحفظ تنتقل إلى رابط `wa.me` برسالة تأكيد جاهزة للإرسال.

## 6. اختبار الإشعار قبل بدء التشغيل

بعد إكمال Properties والموافقة على القالب، اختاري الدالة `testWhatsAppBookingNotification` من محرر Apps Script ثم اضغطي **Run**. هذه الدالة ترسل إشعاراً مكتوباً بوضوح على أنه **اختبار فقط — لا يوجد موعد فعلي** إلى الرقم المحدد في `WHATSAPP_OWNER_PHONE`. افتحي **Execution log** للتحقق من النتيجة.

| نتيجة السجل | المعنى والإجراء |
|---|---|
| `"sent":true` مع `messageId` | نجح إرسال طلب الإشعار إلى Meta. افحصي WhatsApp على رقم الإدارة. |
| `WHATSAPP_NOT_CONFIGURED` | أكملي خاصية أو أكثر من Script Properties المذكورة في القسم 3. |
| `WHATSAPP_API_401` أو `WHATSAPP_API_403` | راجعي رمز الوصول وصلاحياته وحالة رقم الأعمال في Meta. |
| `WHATSAPP_API_400` | غالباً اسم القالب أو لغته أو عدد المتغيرات لا يطابق القالب المعتمد. |
| `WHATSAPP_REQUEST_FAILED` | راجعي Execution log، ثم تحققي من اتصال المشروع وخدمة Meta. |

بعد نجاح الاختبار، أنشئي حجزاً تجريبياً من الموقع. في حالة الحجز الجديد فقط يرسل السكربت الإشعار الآلي. لا يكرر إشعارات واتساب عند تحديث أو إعادة جدولة الحجز حتى لا تصل تنبيهات مكررة إلى الإدارة.

## ملاحظات أمنية وتشغيلية

لا تضعي `WHATSAPP_ACCESS_TOKEN` أو `SALON_WEBHOOK_SECRET` في React أو في رابط عام أو في Google Sheet. يجب تدوير رمز Meta فوراً إذا ظهر في لقطة شاشة أو سجل مشترك. كما يجب التأكد من موافقة الرقم المستلم على تلقي الرسائل من رقم Cloud API المرسل، التزاماً بسياسة مراسلات واتساب.[2]

كل طلب يصل إلى `doPost(e)` يتحقق أولاً من السر المشترك قبل لمس بيانات Google. لا تستخدمي أسماء معاملات محجوزة مثل `c` أو `sid` في طلب Web App، لأنها قد تنتج عنها استجابة HTTP 405 بحسب وثائق Google.[3]

## المراجع

[1]: https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/overview "Meta for Developers — Template fundamentals"
[2]: https://developers.facebook.com/documentation/business-messaging/whatsapp/messages/send-messages "Meta for Developers — Service messages"
[3]: https://developers.google.com/apps-script/guides/web "Google Apps Script — Web Apps"
